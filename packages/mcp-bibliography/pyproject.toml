[project]
name = "biblio-mcp"
version = "0.2.0"
description = "Multi-source scholarly search MCP server: OpenAlex + Scopus + Semantic Scholar + CORE"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27",
]

